[project]
name = "maya"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pipecat-ai[openai]",
]

[tool.uv.sources]
pipecat-ai = { workspace = true }
